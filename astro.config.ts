import { defineConfig } from 'astro/config';
// ここに必要なインテグレーションをインポートします
// 例: import tailwind from '@astrojs/tailwind';

export default defineConfig({
  // インテグレーションの追加
  // integrations: [tailwind()],

  // その他の設定オプションをここに追加します
});
